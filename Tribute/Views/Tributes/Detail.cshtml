@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Tribute Detail";
}

<div>
    <h1>Tribute to @ViewBag.TributeTitle</h1>
    <h5>Description:</h5>
    <div>@Html.Raw(ViewBag.Description)</div>
    @if (ViewBag.Artists.Length > 0)
    {
        <div>
            <ul>
                @foreach (var artist in ViewBag.Artists)
            {
                    <text>
                        <li>@artist</li> </text>
                }
            </ul>
        </div>
    }
        
</div>

